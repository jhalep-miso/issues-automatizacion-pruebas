{
  "name": "kraken-ghost-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --build",
    "pretest": "rimraf features/web && rimraf ghost-*",
    "test": "npm run build && kraken-node run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "kraken-node": "^1.0.24",
    "rimraf": "^5.0.6"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  }
}
